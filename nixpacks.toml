# Railway Deployment Configuration
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_22", "python311"]

[phases.build]
cmds = [
  "npm ci --no-cache --prefer-offline",
  "npm run build"
]

[phases.deploy]
cmd = "npm run start"

[variables]
NODE_ENV = "production"